#!/bin/bash

# Скрипт деплоя для Linux сервера
# Использование: ./deploy.sh

set -e  # Остановка при ошибке

echo "🚀 Начало деплоя..."

# Проверка Node.js
if ! command -v node &> /dev/null; then
    echo "❌ Node.js не установлен. Установите Node.js >= 16.8.0"
    exit 1
fi

NODE_VERSION=$(node -v | cut -d'v' -f2 | cut -d'.' -f1)
if [ "$NODE_VERSION" -lt 16 ]; then
    echo "❌ Требуется Node.js >= 16.8.0. Текущая версия: $(node -v)"
    exit 1
fi

echo "✅ Node.js версия: $(node -v)"

# Установка зависимостей
echo "📦 Установка зависимостей..."
npm install --legacy-peer-deps --production=false

# Сборка проекта
echo "🔨 Сборка проекта..."
npm run build

# Проверка наличия директории out
if [ ! -d "out" ]; then
    echo "❌ Директория out не найдена. Сборка не удалась."
    exit 1
fi

echo "✅ Сборка завершена успешно!"
echo "📁 Статические файлы находятся в директории: out/"
echo ""
echo "Для запуска сервера используйте:"
echo "  npm start"
echo "или"
echo "  PORT=8000 node server.js"

