# Инструкция по использованию офлайн-версии React.dev

## 🚀 Быстрый старт

### Вариант 1: Node.js сервер (рекомендуется)
```bash
node server.js
```
Откройте http://localhost:8000/learn/

### Вариант 2: Python сервер
```bash
cd out
python3 -m http.server 8000
```

### Вариант 3: PHP сервер
```bash
cd out
php -S localhost:8000
```

## 🔄 После изменений в коде

После любых изменений в файлах `.md` или других файлах:

```bash
./rebuild.sh
```

Или вручную:
```bash
npx yarn build
```

## 📝 Где находятся ссылки на архивы

Ссылки добавлены в файл: `src/content/learn/index.md` (строка 80)

Слова **"standard"** и **"node_modules"** являются ссылками на скачивание архивов:
- `/downloads/standard.zip` - архив из папки "standard version"
- `/downloads/node_modules.zip` - архив из папки "node_modules version"

## 🐛 Исправление проблем

### Проблема: При обновлении страницы показывается directory listing

**Решение:** Используйте Node.js сервер (`node server.js`) вместо простого Python сервера. Он правильно обрабатывает роутинг.

### Проблема: Не вижу изменения на сайте

**Решение:** После изменений обязательно пересоберите сайт:
```bash
./rebuild.sh
```

### Проблема: Ссылки на архивы не работают

**Решение:** Убедитесь, что архивы скопированы в `out/downloads/`:
```bash
mkdir -p out/downloads
cp public/downloads/*.zip out/downloads/
```

## 💾 Запоминание страницы

Сайт автоматически запоминает последнюю открытую страницу в `localStorage`. При следующем открытии можно восстановить позицию.

## 🔧 Перенос на обычный React

**Можно ли перенести на обычный React?**

Да, но это потребует:
1. Переписать всю систему роутинга (сейчас используется Next.js)
2. Переписать систему компиляции MDX
3. Настроить сборку с Webpack/Vite
4. Потерять оптимизации Next.js

**Рекомендация:** Оставить Next.js, так как:
- Уже настроен и работает
- Поддерживает статический экспорт
- Имеет встроенную оптимизацию
- Проще поддерживать

## 📁 Структура проекта

- `src/content/learn/index.md` - главный файл с документацией (здесь ссылки)
- `public/downloads/` - архивы для скачивания
- `out/` - собранная статическая версия
- `server.js` - Node.js сервер для локального запуска

